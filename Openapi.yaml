openapi: 3.0.3
info:
  title: BMS Platform Microservices API
  description: Unified OpenAPI 3.0.3 Specification for Building Management System (BMS) Microservices (Auth, Users, Invitations, Notifications, Tickets, Reports, Alarms, Tenants/Organizations, Zones, Areas, Sites, Buildings, Assets, Devices, Telemetry).
  version: 1.0.0
servers:
  - url: http://localhost:3001/api/v1
    description: Local Development Gateway
  - url: https://api.bms.com/v1
    description: Production Gateway

security:
  - BearerAuth: []

tags:
  - name: Auth
    description: Authentication & Identity Management
  - name: Users
    description: User Profile & Role Administration
  - name: Invitations
    description: User Invitation & Access Delegation
  - name: Notifications
    description: Multi-channel Email, SMS & In-App Alerts
  - name: Companies
    description: SAAS Company Administration
  - name: Tenants
    description: Multi-tenant Organization Administration
  - name: Zones
    description: Geographic Zone Management
  - name: TenantAreas
    description: Area Administration within Zones
  - name: Sites
    description: Physical Site & Location Management
  - name: Buildings
    description: Building Management within Sites
  - name: Assets
    description: Hierarchical Asset Tree (Building -> Floor -> Room -> Equipment)
  - name: DeviceProfiles
    description: Device Profile Templates, Field Mappings, Default Thresholds & Enums
  - name: Devices
    description: Device Provisioning, Inventory, Templates, Settings & Status
  - name: Widgets
    description: Device Widget Panels, Metric Configurations & Display Ordering
  - name: Rules
    description: Device Condition, Comparison & Sochiot Automation Rules
  - name: Commands
    description: Hardware Commands & Control Pipeline
  - name: Telemetry
    description: Live & Historical Sensor Telemetry Data
  - name: Tickets
    description: Maintenance & Support Ticket Lifecycle
  - name: Reports
    description: Telemetry & DPR Report Generation
  - name: Alarms
    description: Real-time Device Alarm Monitoring & Acknowledgment

paths:
  # ============================================================
  # A. AUTH SERVICE
  # ============================================================
  /auth/login:
    post:
      tags: [Auth]
      summary: User Login
      description: Authenticates user credentials and returns JWT access token & HttpOnly refresh cookie.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
            example:
              email: "sa@ismartaccess.com"
              password: "Password@123"
              rememberMe: true
      responses:
        '200':
          description: Successful Authentication
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /auth/refresh:
    post:
      tags: [Auth]
      summary: Refresh Access Token
      description: Rotates refresh token and issues a new access token.
      security: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                refreshToken:
                  type: string
            example:
              refreshToken: "d9f8e7c6b5a4..."
      responses:
        '200':
          description: Token refreshed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      accessToken:
                        type: string
                        example: "eyJhbGciOiJIUzI1Ni..."
                      refreshToken:
                        type: string
                        example: "7f9a8b1c2d3e4f5a6b7c8d9e0f..."
                      expiresIn:
                        type: integer
                        example: 900
        '401':
          $ref: '#/components/responses/Unauthorized'

  /auth/logout:
    post:
      tags: [Auth]
      summary: Logout User
      description: Revokes current refresh token and clears auth cookies.
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Logged out successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /auth/forgot-password:
    post:
      tags: [Auth]
      summary: Request Password Reset
      description: Generates reset token and sends password reset email.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
            example:
              email: "user@example.com"
      responses:
        '200':
          description: Password reset request processed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /auth/reset-password:
    post:
      tags: [Auth]
      summary: Reset Password With Token
      description: Resets password using the received reset token or code.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, newPassword]
              properties:
                token:
                  type: string
                newPassword:
                  type: string
                  minLength: 8
            example:
              token: "tok_reset_12345"
              newPassword: "NewPassword@123"
      responses:
        '200':
          description: Password reset successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /auth/verify-email:
    post:
      tags: [Auth]
      summary: Verify Email Address
      description: Verifies user account using verification token.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token]
              properties:
                token:
                  type: string
            example:
              token: "v9u8t7s6r5q4..."
      responses:
        '200':
          description: Email verified successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /auth/me:
    get:
      tags: [Auth]
      summary: Get Current User Profile
      responses:
        '200':
          description: Current user profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'

  # ============================================================
  # B. USERS SERVICE
  # ============================================================
  /users:
    get:
      tags: [Users]
      summary: List Users
      description: Retrieve paginated list of users with role, status, and tenant filtering.
      parameters:
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
        - name: role
          in: query
          schema:
            $ref: '#/components/schemas/UserRole'
        - name: status
          in: query
          schema:
            $ref: '#/components/schemas/UserStatus'
      responses:
        '200':
          description: Paginated users list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/User'
    post:
      tags: [Users]
      summary: Create User
      description: Admin endpoint to create a user with multi-node zoneLocations and trigger an email invitation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRequest'
            example:
              name: "Rajesh Padhi"
              email: "rajesh@sochiot.com"
              role: "ADMIN"
              roleId: 2
              tenantId: "c2a8b410-449e-11ee-be56-0242ac120002"
              zoneLocations:
                - zoneNodeType: "ZONE"
                  zoneNodeId: "18"
                - zoneNodeType: "SITE"
                  zoneNodeId: "7"
                - zoneNodeType: "ASSET"
                  zoneNodeId: "room_101"
      responses:
        '201':
          description: User created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'

  /users/{id}:
    get:
      tags: [Users]
      summary: Get User Details
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: User details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [Users]
      summary: Update User
      description: Updates user details, such as name, email, password, role, status, scope, or feature permissions.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserRequest'
            example:
              name: "Rajesh Padhi Updated"
              password: "NewUserPassword123!"
              role: "SUPER_ADMIN"
      responses:
        '200':
          description: User updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
    delete:
      tags: [Users]
      summary: Soft Delete User
      description: Soft deletes the user by setting status to INACTIVE, setting deletedAt timestamp, clearing sochiotUserId, and appending a timestamp to the email address (e.g. user@example.com_deleted_1723456789) to avoid unique constraint conflicts on future re-creation.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: User soft-deleted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /users/{id}/change-password:
    post:
      tags: [Users]
      summary: Admin Change User Password
      description: Allows an admin to forcefully change or reset another user's password.
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Target User ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password]
              properties:
                password:
                  type: string
                  minLength: 6
                  description: New password for the user
            example:
              password: "NewUserPassword123!"
      responses:
        '200':
          description: Password updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Password updated successfully"
                  data:
                    $ref: '#/components/schemas/User'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'

  /users/{id}/unlock:
    post:
      tags: [Users]
      summary: Admin Unlock User Account
      description: Unlocks a user account by clearing Redis lockout state and setting user status to ACTIVE in database.
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Target User ID
          schema:
            type: string
      responses:
        '200':
          description: User account unlocked successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "User account unlocked successfully"
                  data:
                    $ref: '#/components/schemas/User'
        '404':
          $ref: '#/components/responses/NotFound'

  # ============================================================
  # B1. INVITATIONS SERVICE
  # ============================================================
  /invitations:
    post:
      tags: [Invitations]
      summary: Send User Invitation # Inferred from model
      description: Issues an email invitation token to a new user for account setup and access delegation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateInvitationRequest'
            example:
              email: "invitee@example.com"
              role: "OPERATOR"
              invitedBy: "Admin User"
      responses:
        '201':
          description: Invitation sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invitation'

  /invitations/{token}:
    get:
      tags: [Invitations]
      summary: Get Invitation Details # Inferred from model
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Invitation payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invitation'

  /invitations/{token}/accept:
    post:
      tags: [Invitations]
      summary: Accept Invitation # Inferred from model
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password, name]
              properties:
                name:
                  type: string
                password:
                  type: string
                  minLength: 8
            example:
              name: "Accepted User"
              password: "Password@123"
      responses:
        '200':
          description: Invitation accepted and account activated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'

  /invitations/{token}/decline:
    post:
      tags: [Invitations]
      summary: Decline Invitation # Inferred from model
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Invitation declined
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  # ============================================================
  # B2. COMPANIES SERVICE
  # ============================================================
  /companies:
    get:
      tags: [Companies]
      summary: Get all SAAS companies
      description: Retrieves a paginated list of SAAS companies (SuperAdmin only).
      parameters:
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
        - name: status
          in: query
          description: Filter by company status
          schema:
            $ref: '#/components/schemas/CompanyStatus'
        - name: search
          in: query
          description: Search by company name or code
          schema:
            type: string
      responses:
        '200':
          description: Paginated companies list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Company'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Companies]
      summary: Create a new SAAS company
      description: Creates a new SAAS company record in BMS (SuperAdmin only).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCompanyRequest'
            example:
              name: "Acme Corporation"
              code: "ACME"
              email: "contact@acme.com"
              phone: "+1-555-0199"
              address: "123 Business Way, Suite 100"
              sochiotOrgId: 101
              status: "ACTIVE"
      responses:
        '201':
          description: Company created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Company'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /companies/{id}:
    get:
      tags: [Companies]
      summary: Get company by ID
      description: Retrieves company details by ID (SuperAdmin only).
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Company details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Company'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [Companies]
      summary: Update company
      description: Updates company details by ID (SuperAdmin only).
      parameters:
        - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCompanyRequest'
            example:
              name: "Acme Corporation Updated"
              phone: "+1-555-0200"
      responses:
        '200':
          description: Company updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Company'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [Companies]
      summary: Delete company
      description: Soft deletes a company by ID (SuperAdmin only).
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Company deleted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /companies/{id}/tenants:
    get:
      tags: [Companies]
      summary: Get tenants under company
      description: Retrieves paginated tenants belonging to the specified company (SuperAdmin only).
      parameters:
        - $ref: '#/components/parameters/IdPath'
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
      responses:
        '200':
          description: Paginated tenant list under company
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Tenant'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  # ============================================================
  # C. TENANTS / ORGANIZATIONS SERVICE
  # ============================================================
  /tenants:
    get:
      tags: [Tenants]
      summary: List Organizations / Tenants
      description: Retrieve paginated list of tenants/organizations. Super Admins receive all tenants; regular users receive only their assigned tenant/organization.
      parameters:
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
      responses:
        '200':
          description: Paginated tenant list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Tenant'
    post:
      tags: [Tenants]
      summary: Create Organization / Tenant
      description: Creates a new tenant/organization in BMS (Super Admin required).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTenantRequest'
            example:
              companyId: "cmp_12345"
              name: "Sochiot Technologies"
              email: "info@sochiot.com"
              phone: "+91-1234567890"
              address: "Sector 63, Noida"
              sochiotOrgId: 7
              subscription: "BASIC"
      responses:
        '201':
          description: Tenant created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tenant'

  /tenants/{id}:
    get:
      tags: [Tenants]
      summary: Get Tenant Details
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Tenant details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tenant'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [Tenants]
      summary: Update Tenant
      description: Update tenant details (Super Admin required).
      parameters:
        - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTenantRequest'
            example:
              name: "Sochiot Technologies Ltd"
              phone: "+91-9876543210"
      responses:
        '200':
          description: Tenant updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tenant'
    delete:
      tags: [Tenants]
      summary: Soft Delete Tenant
      description: Soft deletes tenant by setting status to INACTIVE (Super Admin required).
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Tenant soft-deleted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /tenants/{id}/features:
    patch:
      tags: [Tenants]
      summary: Update Tenant Features
      description: Update feature permission flags for a tenant (Super Admin required).
      parameters:
        - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                features:
                  type: object
            example:
              features:
                alarm: true
                reports: true
                dpr: true
      responses:
        '200':
          description: Tenant features updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tenant'

  /tenants/{id}/subscription:
    patch:
      tags: [Tenants]
      summary: Update Tenant Subscription
      description: Update subscription type, period, or license validity (Super Admin required).
      parameters:
        - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subscription:
                  type: string
                  enum: [BASIC, PREMIUM, ENTERPRISE]
                subscriptionPeriod:
                  type: string
                  enum: [MONTHLY, QUARTERLY, ANNUALLY]
                licenseValidity:
                  type: string
                  format: date-time
            example:
              subscription: "PREMIUM"
              subscriptionPeriod: "ANNUALLY"
      responses:
        '200':
          description: Tenant subscription updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tenant'

  /tenants/{id}/dashboard:
    get:
      tags: [Tenants]
      summary: Get Tenant Overview Dashboard
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: High-level tenant dashboard metrics across sites and devices
          content:
            application/json:
              schema:
                type: object

  /tenants/{id}/reactivate:
    post:
      tags: [Tenants]
      summary: Reactivate Tenant
      description: Reactivates a soft-deleted tenant by clearing deletedAt, setting status to ACTIVE, and restoring associated zones and areas (Super Admin required).
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Tenant reactivated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tenant'
        '404':
          $ref: '#/components/responses/NotFound'

  # ============================================================
  # D. ZONES SERVICE
  # ============================================================
  /zones:
    get:
      tags: [Zones]
      summary: List Geographic Zones
      parameters:
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
        - name: tenantId
          in: query
          schema:
            type: string
      responses:
        '200':
          description: List of zones
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Zone'
    post:
      tags: [Zones]
      summary: Create Zone
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateZoneRequest'
            example:
              tenantId: "c2a8b410-449e-11ee-be56-0242ac120002"
              name: "Noida Region"
              region: "North India"
              timezone: "Asia/Kolkata"
      responses:
        '201':
          description: Zone created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Zone'

  /zones/{id}:
    get:
      tags: [Zones]
      summary: Get Zone Details
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Zone details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Zone'
    patch:
      tags: [Zones]
      summary: Update Zone
      description: Update zone details such as name, region, timezone, country, description, or status.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateZoneRequest'
            example:
              name: "Noida Region Updated"
              region: "North India"
              timezone: "Asia/Kolkata"
      responses:
        '200':
          description: Zone updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Zone'
    delete:
      tags: [Zones]
      summary: Soft Delete Zone
      description: Soft deletes the zone (sets status to INACTIVE and sets deletedAt timestamp) and soft-deletes associated areas.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Zone soft-deleted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Zone'

  /zones/{id}/reactivate:
    post:
      tags: [Zones]
      summary: Reactivate Zone
      description: Reactivates a soft-deleted zone by clearing deletedAt, setting status to ACTIVE, and restoring associated areas.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Zone reactivated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Zone'
        '404':
          $ref: '#/components/responses/NotFound'

  # ============================================================
  # E. TENANT AREAS SERVICE
  # ============================================================
  /areas:
    get:
      tags: [TenantAreas]
      summary: List Tenant Areas
      parameters:
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
        - name: zoneId
          in: query
          schema:
            type: string
      responses:
        '200':
          description: List of areas
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/TenantArea'
    post:
      tags: [TenantAreas]
      summary: Create Area
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTenantAreaRequest'
            example:
              tenantId: "c2a8b410-449e-11ee-be56-0242ac120002"
              zoneId: "zone_noida"
              name: "Sector 63"
              description: "Industrial Sector 63"
      responses:
        '201':
          description: Area created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantArea'

  /areas/{id}:
    get:
      tags: [TenantAreas]
      summary: Get Area Details
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Area details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantArea'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [TenantAreas]
      summary: Update Area
      description: Update area details such as name, description, features, dashboardTemplate, or status.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTenantAreaRequest'
            example:
              name: "Sector 63 Updated"
              description: "Updated Sector 63 description"
      responses:
        '200':
          description: Area updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantArea'
    delete:
      tags: [TenantAreas]
      summary: Soft Delete Area
      description: Soft deletes the area (sets status to INACTIVE and sets deletedAt timestamp) and soft-deletes associated sites.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Area soft-deleted successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantArea'

  /areas/{id}/reactivate:
    post:
      tags: [TenantAreas]
      summary: Reactivate Area
      description: Reactivates a soft-deleted area by clearing deletedAt, setting status to ACTIVE, and restoring associated sites.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Area reactivated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantArea'
        '404':
          $ref: '#/components/responses/NotFound'

  /areas/{id}/hierarchy:
    get:
      tags: [TenantAreas]
      summary: Get Area Hierarchy
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Area hierarchy (area -> sites -> devices)
          content:
            application/json:
              schema:
                type: object

  /areas/{id}/stats:
    get:
      tags: [TenantAreas]
      summary: Get Area Statistics
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Area statistics summary
          content:
            application/json:
              schema:
                type: object

  # ============================================================
  # F. SITES SERVICE
  # ============================================================
  /sites:
    get:
      tags: [Sites]
      summary: List Sites
      parameters:
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
      responses:
        '200':
          description: List of sites
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Site'
    post:
      tags: [Sites]
      summary: Create Site
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSiteRequest'
            example:
              name: "Noida Testing Site"
              sochiotLocationId: 7
              organizationId: 7
              tenantId: "c2a8b410-449e-11ee-be56-0242ac120002"
              city: "Noida"
              state: "Uttar Pradesh"
      responses:
        '201':
          description: Site created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Site'

  /sites/{id}:
    get:
      tags: [Sites]
      summary: Get Site Details
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Site details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Site'
        '403':
          description: Forbidden - User does not have access to this site
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Site not found or inactive
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      tags: [Sites]
      summary: Update Site
      description: Update site details such as name, address, city, state, timezone, multi-tenant hierarchy mapping, or active status.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSiteRequest'
            example:
              name: "Noida Testing Site Updated"
              city: "Noida"
              state: "Uttar Pradesh"
              isActive: true
      responses:
        '200':
          description: Site updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Site'
    delete:
      tags: [Sites]
      summary: Soft Delete Site
      description: Soft deletes the site by disabling it (setting isActive = false) and disabling all associated active devices (setting isActive = false).
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Site soft-deleted and disabled successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Site'

  /sites/{id}/stats:
    get:
      tags: [Sites]
      summary: Get Site Stats & Metrics
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Site device count, active alarms, energy metrics

  /sites/{siteId}/telemetry/resync:
    post:
      tags: [Telemetry]
      summary: Resync Telemetry Data
      description: Manual trigger to re-aggregate raw MongoDB events into PostgreSQL rollups for a site.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [date]
              properties:
                date:
                  type: string
                  format: date
                  example: "2026-07-24"
      responses:
        '200':
          description: Resync process initiated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  # ============================================================
  # F1. BUILDINGS SERVICE
  # ============================================================
  /sites/{siteId}/buildings:
    get:
      tags: [Buildings]
      summary: List Buildings For Site
      description: Retrieve all buildings associated with a specific site.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: List of buildings
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Building'
    post:
      tags: [Buildings]
      summary: Create Building In Site
      description: Create a new building under a site.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBuildingRequest'
            example:
              name: "Main Tower A"
              code: "BLD-A"
              totalFloors: 5
              description: "Primary corporate office tower"
              isActive: true
              displayOrder: 1
      responses:
        '201':
          description: Building created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Building'
        '400':
          description: Bad Request - Site is inactive or invalid data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Site not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /sites/{siteId}/buildings/{buildingId}:
    get:
      tags: [Buildings]
      summary: Get Building Details
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: buildingId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Building details
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Building'
    patch:
      tags: [Buildings]
      summary: Update Building
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: buildingId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBuildingRequest'
            example:
              name: "Main Tower A Updated"
              totalFloors: 6
      responses:
        '200':
          description: Building updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Building'
    delete:
      tags: [Buildings]
      summary: Delete Building
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: buildingId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Building deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  # ============================================================
  # G. ASSETS SERVICE (Building -> Floor -> Area -> Cubicle -> Equipment)
  # ============================================================
  /sites/{siteId}/assets:
    get:
      tags: [Assets]
      summary: List Assets For Site
      description: Retrieve paginated list of assets under a site with optional filtering by type, status, hierarchy level/depth, path, or search text.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
        - name: assetType
          in: query
          schema:
            $ref: '#/components/schemas/AssetType'
        - name: status
          in: query
          schema:
            $ref: '#/components/schemas/AssetStatus'
        - name: parentId
          in: query
          schema:
            type: string
        - name: depth
          in: query
          description: Exact hierarchy depth level (0 for root, 1 for floor, 2 for area, etc.)
          schema:
            type: integer
        - name: minDepth
          in: query
          schema:
            type: integer
        - name: maxDepth
          in: query
          schema:
            type: integer
        - name: pathPrefix
          in: query
          description: Materialized path prefix to query subtrees (e.g., /building_main/floor_1)
          schema:
            type: string
        - name: search
          in: query
          description: Search term matching asset name, description, or serial number
          schema:
            type: string
      responses:
        '200':
          description: Paginated assets list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Asset'

    post:
      tags: [Assets]
      summary: Create Asset In Site
      description: Create a new asset in the site hierarchy. Automatically calculates depth and materialized path.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAssetRequest'
            example:
              name: "Area 1 - Floor 1"
              assetType: "AREA"
              parentId: "floor_1"
              order: 1
              description: "Open workspace area"
              metadata:
                capacity: 40
                sqft: 2000
      responses:
        '201':
          description: Asset created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Asset'
        '400':
          description: Bad Request - Site is inactive, circular parent, or invalid payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Site or parent asset not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflict - Duplicate asset name under the same parent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /sites/{siteId}/assets/hierarchy:
    get:
      tags: [Assets]
      summary: Get Site Asset Hierarchy Tree
      description: Returns an arbitrary-depth recursive tree of all assets for the site, including nested children and linked devices.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: rootAssetId
          in: query
          description: Optional root asset ID to retrieve subtree from
          schema:
            type: string
      responses:
        '200':
          description: Recursive asset hierarchy tree
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: object

  /sites/{siteId}/assets/stats:
    get:
      tags: [Assets]
      summary: Get Site Asset Statistics
      description: Aggregated statistics of assets by type, status, depth levels, and linked devices.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Asset statistics summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      total:
                        type: integer
                      byType:
                        type: object
                      byStatus:
                        type: object
                      byDepth:
                        type: object
                      devices:
                        type: integer

  /sites/{siteId}/assets/types/{assetType}:
    get:
      tags: [Assets]
      summary: Get Assets By Type
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: assetType
          in: path
          required: true
          schema:
            $ref: '#/components/schemas/AssetType'
      responses:
        '200':
          description: List of assets matching the specified type
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Asset'

  /assets:
    get:
      tags: [Assets]
      summary: List Assets Across Accessible Sites
      description: Returns all assets across all sites accessible to the authenticated user based on their authtoken and permissions. If the `siteId` query parameter is provided, verifies user site access permission and returns assets for that specific site (or empty array if access is not permitted).
      parameters:
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
        - name: siteId
          in: query
          description: Optional site ID to filter assets. User must have access permission to this site.
          schema:
            type: integer
            example: 7
        - name: assetType
          in: query
          schema:
            $ref: '#/components/schemas/AssetType'
        - name: status
          in: query
          schema:
            $ref: '#/components/schemas/AssetStatus'
        - name: search
          in: query
          description: Search term matching asset name, description, or serial number
          schema:
            type: string
        - name: parentId
          in: query
          description: Parent asset ID (or 'null' for root assets)
          schema:
            type: string
        - name: depth
          in: query
          description: Exact hierarchy depth level (0 for root, 1 for floor, 2 for area, etc.)
          schema:
            type: integer
        - name: minDepth
          in: query
          schema:
            type: integer
        - name: maxDepth
          in: query
          schema:
            type: integer
        - name: pathPrefix
          in: query
          description: Materialized path prefix to query subtrees (e.g., /building_main/floor_1)
          schema:
            type: string
        - name: includeChildren
          in: query
          schema:
            type: boolean
            default: false
      responses:
        '200':
          description: Paginated assets list with site details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Asset'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [Assets]
      summary: Create Asset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAssetRequest'
            example:
              siteId: 7
              name: "Main Building"
              assetType: "BUILDING"
              description: "Primary testing building"
      responses:
        '201':
          description: Asset created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Asset'

  /assets/{id}:
    get:
      tags: [Assets]
      summary: Get Asset Details
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Asset details
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Asset'
        '404':
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    patch:
      tags: [Assets]
      summary: Update Asset
      description: Update asset properties. If parentId changes, depth and materialized paths of the asset and all its subtree descendants are automatically recalculated.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAssetRequest'
            example:
              name: "Area 1 - Floor 1 (Renovated)"
              order: 2
              metadata:
                capacity: 60
                sqft: 2400
      responses:
        '200':
          description: Asset updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Asset'
        '400':
          description: Bad Request - Circular hierarchy reference or invalid payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Asset or new parent not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

    delete:
      tags: [Assets]
      summary: Soft Delete Asset
      description: Soft deletes the asset and all its recursive subtree descendants. Rejects if active devices are still attached.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Asset and subtree soft-deleted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Asset'
        '400':
          description: Bad Request - Asset or descendants have associated active devices
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Asset not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /assets/{id}/tree:
    get:
      tags: [Assets]
      summary: Get Asset Hierarchy Subtree
      description: Returns recursive child asset subtree for a specific asset node.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: Recursive asset subtree
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: object

  /assets/{id}/devices:
    get:
      tags: [Assets]
      summary: Get All Devices Under Asset
      description: Retrieves all active devices linked to this asset or any of its recursive child assets in the subtree.
      parameters:
        - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: List of devices under the asset subtree
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Device'

  # ============================================================
  # H. DEVICE PROFILES, DEVICES, WIDGETS, RULES & COMMANDS SERVICE
  # ============================================================
  /profiles:
    get:
      tags: [DeviceProfiles]
      summary: List Device Profiles
      description: Retrieves paginated device profiles with active linked device summaries. Supports filtering by active status and category.
      parameters:
        - name: page
          in: query
          description: Page number (1-based)
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          description: Number of items per page (max 100)
          schema:
            type: integer
            default: 20
        - name: isActive
          in: query
          description: Filter profiles by active status
          schema:
            type: boolean
        - name: category
          in: query
          description: Filter profiles by device category
          schema:
            $ref: '#/components/schemas/DeviceCategory'
      responses:
        '200':
          description: Paginated list of device profiles
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceProfile'
                  meta:
                    $ref: '#/components/schemas/PaginationMeta'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags: [DeviceProfiles]
      summary: Create Device Profile
      description: Creates a new device profile schema defining field mappings, default telemetry thresholds, and dashboard config. Requires ADMIN permission. Category must be unique.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProfileRequest'
            example:
              name: "Schneider EM6400NG Energy Meter"
              category: "ENERGY_METER"
              functionalSystem: "ELECTRICAL"
              description: "Standard 3-phase multi-function power meter"
              fieldMappings:
                voltage_r_phase:
                  displayName: "R-Phase Voltage"
                  unit: "V"
                  dataType: "FLOAT"
                  isCommand: false
                  graphable: true
                  displayOrder: 1
                  enumValues: []
                power_total:
                  displayName: "Total Active Power"
                  unit: "kW"
                  dataType: "FLOAT"
                  isCommand: false
                  graphable: true
                  displayOrder: 2
                  enumValues: []
              defaultThresholds:
                voltage_r_phase:
                  warningHigh: 250
                  criticalHigh: 260
                  warningLow: 210
                  criticalLow: 200
              dashboardConfig:
                primaryField: "power_total"
                gaugeFields: ["voltage_r_phase"]
                chartFields: ["power_total"]
              isActive: true
      responses:
        '200':
          description: Device profile created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'

  /profiles/enums:
    get:
      tags: [DeviceProfiles]
      summary: Get All Profile Enums
      description: Returns all supported device categories and functional systems with their identifiers and human-readable display names.
      responses:
        '200':
          description: Supported device categories and functional systems
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/ProfileEnumsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /profiles/enums/categories:
    get:
      tags: [DeviceProfiles]
      summary: Get Device Category Enums
      description: Returns all supported device categories with identifiers and formatted display names.
      responses:
        '200':
          description: List of supported device categories
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/EnumItem'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /profiles/enums/systems:
    get:
      tags: [DeviceProfiles]
      summary: Get Functional System Enums
      description: Returns all supported functional systems with identifiers and formatted display names.
      responses:
        '200':
          description: List of supported functional systems
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/EnumItem'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /profiles/{profileId}:
    get:
      tags: [DeviceProfiles]
      summary: Get Device Profile By ID
      description: Retrieves full details of a specific device profile including field mappings, default thresholds, dashboard configuration, and active linked devices.
      parameters:
        - name: profileId
          in: path
          required: true
          description: Unique CUID identifier of the device profile
          schema:
            type: string
          example: "cm71abcdef0123456789"
      responses:
        '200':
          description: Device profile details
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceProfile'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags: [DeviceProfiles]
      summary: Update Device Profile
      description: Updates an existing device profile's attributes, field mappings, thresholds, or dashboard configuration. Requires ADMIN permission.
      parameters:
        - name: profileId
          in: path
          required: true
          description: Unique CUID identifier of the device profile
          schema:
            type: string
          example: "cm71abcdef0123456789"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProfileRequest'
            example:
              name: "Updated Schneider EM6400NG Energy Meter"
              description: "Updated 3-phase meter configuration"
              dashboardConfig:
                primaryField: "power_total"
                statusField: "status"
                gaugeFields: ["voltage_r_phase", "current_r_phase"]
                chartFields: ["power_total", "energy_today"]
      responses:
        '200':
          description: Device profile updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    delete:
      tags: [DeviceProfiles]
      summary: Delete (Deactivate) Device Profile
      description: Soft deletes a device profile by setting isActive to false. Requires ADMIN permission.
      parameters:
        - name: profileId
          in: path
          required: true
          description: Unique CUID identifier of the device profile
          schema:
            type: string
          example: "cm71abcdef0123456789"
      responses:
        '200':
          description: Device profile deactivated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceProfile'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /devices:
    get:
      tags: [Devices]
      summary: List Devices Across Sites
      description: Retrieves paginated devices across all accessible sites. Supports filtering by category, area, building, active status, search text, or specific siteId (Super Admin).
      parameters:
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
        - name: category
          in: query
          schema:
            $ref: '#/components/schemas/DeviceCategory'
        - name: areaId
          in: query
          schema:
            type: integer
        - name: buildingId
          in: query
          schema:
            type: integer
        - name: siteId
          in: query
          schema:
            type: integer
        - name: isActive
          in: query
          schema:
            type: boolean
        - name: search
          in: query
          schema:
            type: string
        - name: templateName
          in: query
          schema:
            type: string
        - name: include
          in: query
          description: Comma-separated list of relations to include (e.g. `settings,rules,profile,area,building,energyGroup` or `none`).
          schema:
            type: string
            example: "settings,rules,profile"
      responses:
        '200':
          description: Paginated devices list across sites
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Device'

  /devices/{deviceId}/events/latest:
    get:
      tags: [Devices]
      summary: Get Latest Device Events Directly
      description: Retrieves the most recent telemetry event from MongoDB for each module of the device by internal device ID, Sochiot device ID, or BMS device ID, formatted with device settings.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema:
            type: string
          description: Internal Device ID, Sochiot Device ID, or BMS Device ID
          example: "1277"
      responses:
        '200':
          description: Latest formatted device events per module
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceLatestEventModule'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /resync-event-stats:
    post:
      tags: [Devices]
      summary: Global Device Event & Telemetry Resync
      description: Resyncs device raw telemetry events from MongoDB into PostgreSQL rollups starting from a given date. Requires PERM_INST_MANAGE_DEVICE permission.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResyncEventStatsRequest'
            example:
              id: 10
              dateTime: "2026-07-24T00:00:00.000Z"
      responses:
        '200':
          description: Resync completed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /sites/{siteId}/devices:
    get:
      tags: [Devices]
      summary: List Devices For Site
      description: Retrieves paginated list of devices in the site matching filter criteria (category, area, building, status, template). Defaults to returning full rich details including profile, area, building, energyGroup, settings, and rules.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
        - name: category
          in: query
          schema:
            $ref: '#/components/schemas/DeviceCategory'
        - name: areaId
          in: query
          schema:
            type: integer
        - name: buildingId
          in: query
          schema:
            type: integer
        - name: isActive
          in: query
          schema:
            type: boolean
        - name: search
          in: query
          schema:
            type: string
        - name: templateName
          in: query
          schema:
            type: string
        - name: include
          in: query
          description: "Comma-separated list of relations to include (defaults to full: `profile,area,building,energyGroup,settings,rules`). Use `none` or specific names (`settings,rules`) to control payload size."
          schema:
            type: string
            example: "settings,rules"
      responses:
        '200':
          description: List of site devices
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Device'

    post:
      tags: [Devices]
      summary: Provision New Device
      description: Create a new device under the site with Sochiot device IDs and profile linkage.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDeviceRequest'
            example:
              name: "EM_LIVEWIZE_178"
              serialNumber: "9454c5f385e821"
              category: "ENERGY_METER"
              profileId: "prf_12345"
              sochiotDeviceIds: [1231]
              areaId: 1
              buildingId: 1
      responses:
        '201':
          description: Device created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Device'

  /sites/{siteId}/devices/from-template:
    post:
      tags: [Devices]
      summary: Create Device From Template
      description: Provision a device using the full frontend template payload with pre-configured settings, thresholds, and optional rules.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceTemplateRequest'
      responses:
        '201':
          description: Device and settings created from template
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Device'

  /sites/{siteId}/devices/templates:
    get:
      tags: [Devices]
      summary: Get Site Template Names
      description: Returns distinct template names configured on devices within this site.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: List of template names
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: string

  /sites/{siteId}/devices/templates/{templateName}:
    get:
      tags: [Devices]
      summary: Get Devices By Template Name
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: templateName
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Devices associated with the specified template
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Device'

  /sites/{siteId}/devices/summary:
    get:
      tags: [Devices]
      summary: Device Summary Statistics
      description: Returns total devices, active/inactive device counts, and category breakdown for the site.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Device summary metrics
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceSummary'

  /sites/{siteId}/devices/recent_events:
    get:
      tags: [Devices]
      summary: Get Recent Events for Site Devices
      description: Fetches recent raw telemetry events for all active devices belonging to the site.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Recent event streams
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: object

  /sites/{siteId}/devices/{deviceId}:
    get:
      tags: [Devices]
      summary: Get Device Details
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Detailed device object including relations
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Device'

    patch:
      tags: [Devices]
      summary: Update Device
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDeviceRequest'
      responses:
        '200':
          description: Device updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Device'

    delete:
      tags: [Devices]
      summary: Soft Delete Device
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Device deleted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Device'

  /sites/{siteId}/devices/{deviceId}/sync:
    post:
      tags: [Devices]
      summary: Sync Device With Config Engine
      description: Re-synchronizes device metadata and field mappings from Sochiot Config Engine.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Device synchronized
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Device'

  /sites/{siteId}/devices/{deviceId}/live:
    get:
      tags: [Devices]
      summary: Get Live Field Values
      description: Returns latest telemetry snapshot and live field values for the device.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Live field telemetry readings
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object

  /sites/{siteId}/devices/{deviceId}/events/latest:
    get:
      tags: [Devices]
      summary: Get Latest Device Events Formatted With Settings
      description: Retrieves the most recent telemetry event from MongoDB for each module of the device, formatted and mapped to device settings.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
          example: 1
        - name: deviceId
          in: path
          required: true
          schema:
            type: string
          description: Internal Device ID, Sochiot Device ID, or BMS Device ID
          example: "1277"
      responses:
        '200':
          description: Latest formatted device events per module
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceLatestEventModule'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /sites/{siteId}/devices/{deviceId}/thresholds:
    patch:
      tags: [Devices]
      summary: Update Device Thresholds
      description: Updates warning/critical low and high threshold overrides for mapped telemetry fields.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateThresholdsRequest'
      responses:
        '200':
          description: Thresholds updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Device'

  /sites/{siteId}/devices/{deviceId}/settings:
    get:
      tags: [Devices]
      summary: Get Device Settings & Field Mappings
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: List of device settings
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceSetting'

    put:
      tags: [Devices]
      summary: Update Device Settings
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDeviceSettingsRequest'
      responses:
        '200':
          description: Settings updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceSetting'

  /sites/{siteId}/devices/{deviceId}/audit-log:
    get:
      tags: [Devices]
      summary: Get Device Audit Action Logs
      description: Paginated log of manual or rule-engine triggered modifications to device settings and controls.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: from
          in: query
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          schema:
            type: string
            format: date-time
        - name: source
          in: query
          schema:
            $ref: '#/components/schemas/ActionSource'
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
      responses:
        '200':
          description: Paginated audit logs
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/ModuleActionLog'

  # ============================================================
  # H1. DEVICE RULES SERVICE
  # ============================================================
  /sites/{siteId}/devices/{deviceId}/rules:
    get:
      tags: [Rules]
      summary: List Device Automation Rules
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: List of rules with field configurations
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceRule'

  /sites/{siteId}/devices/{deviceId}/rules/{ruleId}:
    get:
      tags: [Rules]
      summary: Get Device Rule Details
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: ruleId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Rule details with fields
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceRule'

    put:
      tags: [Rules]
      summary: Update Device Rule
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: ruleId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRuleRequest'
      responses:
        '200':
          description: Rule updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceRule'

    delete:
      tags: [Rules]
      summary: Soft Delete Device Rule
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: ruleId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Rule soft deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /sites/{siteId}/devices/{deviceId}/rules/{ruleId}/fields/{fieldName}:
    patch:
      tags: [Rules]
      summary: Update Single Rule Field Value
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: ruleId
          in: path
          required: true
          schema:
            type: integer
        - name: fieldName
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRuleFieldRequest'
      responses:
        '200':
          description: Field updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceRuleField'

  /sites/{siteId}/devices/{deviceId}/rules/sync:
    post:
      tags: [Rules]
      summary: Sync All Rules From Sochiot
      description: Pulls rules and field definitions from Sochiot for this device.
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Synced rules count and details
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                  count:
                    type: integer
                  rules:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceRule'

  /sites/{siteId}/devices/{deviceId}/rules/{ruleId}/sync:
    post:
      tags: [Rules]
      summary: Sync Specific Rule To Sochiot (By Mapping IDs)
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: ruleId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncRulePayload'
      responses:
        '200':
          description: Rule synced to Sochiot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /sites/{siteId}/devices/{deviceId}/rules/{ruleId}/sync-with-fields:
    post:
      tags: [Rules]
      summary: Sync Specific Rule To Sochiot (By Field Names)
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: ruleId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncRuleWithFieldsRequest'
      responses:
        '200':
          description: Rule mapped and synced to Sochiot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  # ============================================================
  # H2. DEVICE WIDGETS SERVICE
  # ============================================================
  /sites/{siteId}/devices/{deviceId}/widgets:
    get:
      tags: [Widgets]
      summary: List All Widgets For Device
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: List of device widgets
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceWidget'

    post:
      tags: [Widgets]
      summary: Create Widget For Device
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWidgetRequest'
      responses:
        '201':
          description: Widget created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceWidget'

    delete:
      tags: [Widgets]
      summary: Delete All Widgets For Device
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: All device widgets deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /sites/{siteId}/devices/{deviceId}/widgets/bulk:
    post:
      tags: [Widgets]
      summary: Bulk Create Widgets For Device
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkCreateWidgetsRequest'
      responses:
        '201':
          description: Widgets created in bulk
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceWidget'

  /sites/{siteId}/devices/{deviceId}/widgets/active:
    get:
      tags: [Widgets]
      summary: Get Active Widgets For Device
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: List of active widgets
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceWidget'

  /sites/{siteId}/devices/{deviceId}/widgets/paginated:
    get:
      tags: [Widgets]
      summary: Paginated Device Widgets
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: isActive
          in: query
          schema:
            type: boolean
        - name: search
          in: query
          schema:
            type: string
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
      responses:
        '200':
          description: Paginated widgets list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/PaginatedResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/DeviceWidget'

  /sites/{siteId}/devices/{deviceId}/widgets/{widgetId}:
    get:
      tags: [Widgets]
      summary: Get Widget By Device & Widget ID
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: widgetId
          in: path
          required: true
          description: Widget identifier string (e.g., E1, E2)
          schema:
            type: string
      responses:
        '200':
          description: Widget details
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceWidget'

  /sites/{siteId}/devices/{deviceId}/widgets/sync:
    post:
      tags: [Widgets]
      summary: Sync Widgets From Sochiot
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncWidgetsRequest'
      responses:
        '200':
          description: Widgets synced from Sochiot
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceWidget'

  /sites/{siteId}/devices/{deviceId}/widgets/reorder:
    put:
      tags: [Widgets]
      summary: Reorder Widgets
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderWidgetsRequest'
      responses:
        '200':
          description: Widgets reordered
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceWidget'

  /widgets/{widgetId}:
    get:
      tags: [Widgets]
      summary: Standalone Get Widget By Database ID
      parameters:
        - name: widgetId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Widget details
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceWidget'

    patch:
      tags: [Widgets]
      summary: Standalone Update Widget By Database ID
      parameters:
        - name: widgetId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWidgetRequest'
      responses:
        '200':
          description: Widget updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceWidget'

    delete:
      tags: [Widgets]
      summary: Standalone Delete Widget By Database ID
      parameters:
        - name: widgetId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Widget deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  # ============================================================
  # H3. HARDWARE COMMANDS SERVICE
  # ============================================================
  /sites/{siteId}/devices/{deviceId}/commands:
    get:
      tags: [Commands]
      summary: Get Device Command History
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Command execution history for device
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/DeviceCommand'

    post:
      tags: [Commands]
      summary: Send Command To Hardware Device
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendCommandRequest'
            example:
              commandKey: "SET_RELAY_STATE"
              value: "ON"
      responses:
        '200':
          description: Command dispatched to hardware
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceCommand'

  /sites/{siteId}/devices/{deviceId}/commands/{commandId}:
    get:
      tags: [Commands]
      summary: Get Command Status By ID
      parameters:
        - name: siteId
          in: path
          required: true
          schema:
            type: integer
        - name: deviceId
          in: path
          required: true
          schema:
            type: integer
        - name: commandId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Command status and execution log
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/DeviceCommand'

  # ============================================================
  # I. NOTIFICATION SERVICE
  # ============================================================
  /notifications/send:
    post:
      tags: [Notifications]
      summary: Send Notification
      description: Dispatch email, in-app, SMS, or webhook notification.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendNotificationRequest'
            example:
              email: "user@example.com"
              type: "ALARM_TRIGGERED"
              channel: "EMAIL"
              subject: "🚨 Critical Voltage Alarm"
              message: "Device EM_LIVEWIZE_178 reported high voltage."
      responses:
        '200':
          description: Notification dispatched
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessMessageResponse'

  /notifications/in-app:
    get:
      tags: [Notifications]
      summary: List In-App Notifications
      parameters:
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/LimitQuery'
        - name: isRead
          in: query
          schema:
            type: boolean
      responses:
        '200':
          description: Paginated in-app notifications

  # ============================================================
  # J. TICKETS, REPORTS & ALARMS
  # ============================================================
  /reports/generate:
    post:
      tags: [Reports]
      summary: Generate Async Report
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GenerateReportRequest'
      responses:
        '202':
          description: Report job queued

  /alarms/trigger:
    post:
      tags: [Alarms]
      summary: Trigger Alarm Event
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TriggerAlarmRequest'
      responses:
        '201':
          description: Alarm triggered

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    IdPath:
      name: id
      in: path
      required: true
      schema:
        type: string
    PageQuery:
      name: page
      in: query
      schema:
        type: integer
        default: 1
    LimitQuery:
      name: limit
      in: query
      schema:
        type: integer
        default: 10

  responses:
    BadRequest:
      description: Invalid request payload or parameters
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: Authentication failed or token missing
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: Forbidden - Insufficient permissions
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Conflict:
      description: Resource conflict or duplicate entry
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    TooManyRequests:
      description: Rate limit exceeded
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

  schemas:
    # --- ENUMS ---
    UserRole:
      type: string
      enum: [SUPER_ADMIN, ADMIN, MANAGER, OPERATOR, VIEWER]
    UserStatus:
      type: string
      enum: [ACTIVE, INACTIVE, SUSPENDED, PENDING_VERIFICATION]
    CompanyStatus:
      type: string
      enum: [ACTIVE, INACTIVE, SUSPENDED]
    AssetType:
      type: string
      enum: [BUILDING, FLOOR, AREA, ROOM, CUBICLE, EQUIPMENT, MACHINE, LINE, PANEL, DG, LT_ROOM, PUMP_ROOM, OTHER]
    AssetStatus:
      type: string
      enum: [ACTIVE, INACTIVE, MAINTENANCE]
    DeviceCategory:
      type: string
      enum:
        - ENERGY_METER
        - UG_TANK
        - AG_TANK
        - PUMP
        - VALVE
        - GENERATOR
        - LT_PANEL
        - FIRE_PUMP
        - HVAC_CHILLER
        - HVAC_AHU
        - HVAC_COOLING_TOWER
        - VRV
        - AQI_SENSOR
        - BREAKER
        - STP
        - WTP
        - LIFT
        - LIGHTING
        - FIRE_PANEL
        - CONTROLLER
        - SENSOR
        - AC
        - OTHER
    FunctionalSystem:
      type: string
      enum:
        - WATER_SUPPLY
        - SEWAGE
        - WATER_TREATMENT
        - DG_POWER
        - ELECTRICAL
        - FIRE_SAFETY
        - HVAC
        - LIGHTING_CONTROL
        - LIFT_ELEVATOR
        - AIR_QUALITY
        - GENERIC
    AlarmSeverity:
      type: string
      enum: [INFO, WARNING, CRITICAL]
    FieldDataType:
      type: string
      enum: [INTEGER, FLOAT, STRING, BOOLEAN, ENUM]
    TelemetryType:
      type: string
      enum: [AVERAGE, MIN, MAX, SUM, LAST]
    ReportWidgetType:
      type: string
      enum: [OVERALL, INDIVIDUAL, COMPARATIVE]
    SamplingInterval:
      type: string
      enum: [MIN_15, HOURLY, DAILY, MONTHLY]
    ActionSource:
      type: string
      enum: [USER, RULE_ENGINE, SCHEDULE, SYSTEM]
    CommandStatus:
      type: string
      enum: [SENT, ACKNOWLEDGED, FAILED, TIMED_OUT]

    # --- RESPONSES ---
    SuccessMessageResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        message:
          type: string
          example: "Operation completed successfully."

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            path:
              type: string
            timestamp:
              type: string
              format: date-time

    PaginatedResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        page:
          type: integer
          example: 1
        limit:
          type: integer
          example: 10
        total:
          type: integer
          example: 42
        totalPages:
          type: integer
          example: 5

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 42
        page:
          type: integer
          example: 1
        pageSize:
          type: integer
          example: 20
        totalPages:
          type: integer
          example: 3

    # --- ENTITY MODELS ---
    User:
      type: object
      properties:
        id:
          type: string
          example: "usr_123456789"
        name:
          type: string
          example: "Rajesh Padhi"
        email:
          type: string
          format: email
          example: "rajesh@sochiot.com"
        role:
          $ref: '#/components/schemas/UserRole'
        status:
          $ref: '#/components/schemas/UserStatus'
        tenantId:
          type: string
          nullable: true
        zoneLocations:
          type: array
          items:
            type: object
            properties:
              zoneNodeType:
                type: string
                example: "ZONE"
              zoneNodeId:
                type: string
                example: "18"
        createdAt:
          type: string
          format: date-time

    Invitation:
      type: object
      properties:
        id:
          type: string
          example: "inv_987654321"
        email:
          type: string
          format: email
          example: "invitee@example.com"
        role:
          $ref: '#/components/schemas/UserRole'
        token:
          type: string
          example: "tok_invite_abc123"
        invitedBy:
          type: string
          example: "Admin User"
        expiresAt:
          type: string
          format: date-time
        accepted:
          type: boolean
          example: false

    Company:
      type: object
      properties:
        id:
          type: string
          example: "cmp_12345"
        name:
          type: string
          example: "Acme Corporation"
        code:
          type: string
          nullable: true
          example: "ACME"
        email:
          type: string
          format: email
          nullable: true
          example: "contact@acme.com"
        phone:
          type: string
          nullable: true
          example: "+1-555-0199"
        address:
          type: string
          nullable: true
          example: "123 Business Way, Suite 100"
        sochiotOrgId:
          type: integer
          nullable: true
          example: 101
        status:
          $ref: '#/components/schemas/CompanyStatus'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true

    CreateCompanyRequest:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          example: "Acme Corporation"
        code:
          type: string
          example: "ACME"
        email:
          type: string
          format: email
          example: "contact@acme.com"
        phone:
          type: string
          example: "+1-555-0199"
        address:
          type: string
          example: "123 Business Way, Suite 100"
        sochiotOrgId:
          type: integer
          example: 101
        status:
          $ref: '#/components/schemas/CompanyStatus'

    UpdateCompanyRequest:
      type: object
      properties:
        name:
          type: string
          example: "Acme Corporation Updated"
        code:
          type: string
          example: "ACME-UPDATED"
        email:
          type: string
          format: email
          example: "contact@acme.com"
        phone:
          type: string
          example: "+1-555-0199"
        address:
          type: string
          example: "123 Business Way, Suite 100"
        sochiotOrgId:
          type: integer
          example: 101
        status:
          $ref: '#/components/schemas/CompanyStatus'

    Tenant:
      type: object
      properties:
        id:
          type: string
          example: "tenant_sochiot"
        name:
          type: string
          example: "Sochiot Technologies"
        email:
          type: string
          example: "info@sochiot.com"
        sochiotOrgId:
          type: integer
          example: 7

    Zone:
      type: object
      properties:
        id:
          type: string
          example: "zone_noida"
        name:
          type: string
          example: "Noida Region"

    TenantArea:
      type: object
      properties:
        id:
          type: string
          example: "area_sector_63"
        name:
          type: string
          example: "Sector 63"

    Site:
      type: object
      properties:
        id:
          type: integer
          example: 7
        name:
          type: string
          example: "Noida Testing Site"

    Building:
      type: object
      properties:
        id:
          type: string
          example: "bld_123456"
        siteId:
          type: integer
          example: 7
        name:
          type: string
          example: "Main Tower A"
        code:
          type: string
          example: "BLD-A"
        totalFloors:
          type: integer
          example: 5
        description:
          type: string
          example: "Primary corporate office tower"
        isActive:
          type: boolean
          example: true
        displayOrder:
          type: integer
          example: 1
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    Asset:
      type: object
      properties:
        id:
          type: string
          example: "ast_cm71a8bc0001"
        siteId:
          type: integer
          example: 7
        site:
          type: object
          nullable: true
          properties:
            id:
              type: integer
              example: 7
            name:
              type: string
              example: "Noida Testing Site"
        name:
          type: string
          example: "Area 1 - Floor 1"
        assetType:
          $ref: '#/components/schemas/AssetType'
        description:
          type: string
          nullable: true
          example: "Primary workspace area on Floor 1"
        parentId:
          type: string
          nullable: true
          example: "floor_1"
        depth:
          type: integer
          example: 2
        path:
          type: string
          nullable: true
          example: "/building_main/floor_1/ast_cm71a8bc0001"
        order:
          type: integer
          example: 0
        metadata:
          type: object
          nullable: true
          example:
            capacity: 50
            sqft: 2400
        status:
          $ref: '#/components/schemas/AssetStatus'
        installDate:
          type: string
          format: date-time
          nullable: true
        installBy:
          type: string
          nullable: true
        lastVisitDate:
          type: string
          format: date-time
          nullable: true
        firmware:
          type: string
          nullable: true
        serialNumber:
          type: string
          nullable: true
        sochiotDeviceIds:
          type: array
          items:
            type: integer
          example: [1231]
        widgetConfig:
          type: object
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true

    EnumItem:
      type: object
      properties:
        name:
          type: string
          example: "ENERGY_METER"
        displayName:
          type: string
          example: "Energy Meter"

    ProfileEnumsResponse:
      type: object
      properties:
        deviceCategories:
          type: array
          items:
            $ref: '#/components/schemas/EnumItem'
        functionalSystems:
          type: array
          items:
            $ref: '#/components/schemas/EnumItem'

    FieldMapping:
      type: object
      required: [displayName, dataType]
      properties:
        displayName:
          type: string
          example: "R-Phase Voltage"
        unit:
          type: string
          nullable: true
          example: "V"
        dataType:
          $ref: '#/components/schemas/FieldDataType'
        isCommand:
          type: boolean
          default: false
          example: false
        graphable:
          type: boolean
          default: true
          example: true
        displayOrder:
          type: integer
          default: 0
          example: 1
        enumValues:
          type: array
          items:
            type: string
          default: []
          example: []

    FieldThreshold:
      type: object
      properties:
        warningHigh:
          type: number
          nullable: true
          example: 250.0
        criticalHigh:
          type: number
          nullable: true
          example: 260.0
        warningLow:
          type: number
          nullable: true
          example: 210.0
        criticalLow:
          type: number
          nullable: true
          example: 200.0

    DashboardConfig:
      type: object
      properties:
        primaryField:
          type: string
          nullable: true
          example: "power_total"
        statusField:
          type: string
          nullable: true
          example: "status"
        gaugeFields:
          type: array
          items:
            type: string
          example: ["voltage_r_phase"]
        chartFields:
          type: array
          items:
            type: string
          example: ["power_total"]

    DeviceProfile:
      type: object
      properties:
        id:
          type: string
          example: "cm71abcdef0123456789"
        name:
          type: string
          example: "Schneider EM6400NG Energy Meter"
        category:
          $ref: '#/components/schemas/DeviceCategory'
        functionalSystem:
          $ref: '#/components/schemas/FunctionalSystem'
        description:
          type: string
          nullable: true
          example: "Standard 3-phase multi-function power meter"
        fieldMappings:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/FieldMapping'
        defaultThresholds:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/FieldThreshold'
        dashboardConfig:
          $ref: '#/components/schemas/DashboardConfig'
        isActive:
          type: boolean
          example: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        devices:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
                example: 10
              name:
                type: string
                example: "EM_Main_LT_01"
              sochiotDeviceIds:
                type: array
                items:
                  type: integer
                example: [1001]
              siteId:
                type: integer
                nullable: true
                example: 1

    CreateProfileRequest:
      type: object
      required: [name, category, fieldMappings]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: "Schneider EM6400NG Energy Meter"
        category:
          $ref: '#/components/schemas/DeviceCategory'
        functionalSystem:
          $ref: '#/components/schemas/FunctionalSystem'
          default: GENERIC
        description:
          type: string
          nullable: true
          example: "Standard 3-phase multi-function power meter"
        fieldMappings:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/FieldMapping'
        defaultThresholds:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/FieldThreshold'
        dashboardConfig:
          $ref: '#/components/schemas/DashboardConfig'
        isActive:
          type: boolean
          default: true

    UpdateProfileRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
          example: "Updated Schneider EM6400NG Energy Meter"
        category:
          $ref: '#/components/schemas/DeviceCategory'
        functionalSystem:
          $ref: '#/components/schemas/FunctionalSystem'
        description:
          type: string
          nullable: true
        fieldMappings:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/FieldMapping'
        defaultThresholds:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/FieldThreshold'
        dashboardConfig:
          $ref: '#/components/schemas/DashboardConfig'
        isActive:
          type: boolean

    Device:
      type: object
      properties:
        id:
          type: integer
          example: 10
        siteId:
          type: integer
          nullable: true
          example: 7
        name:
          type: string
          example: "EM_LIVEWIZE_178"
        description:
          type: string
          nullable: true
        serialNumber:
          type: string
          nullable: true
          example: "9454c5f385e821"
        category:
          $ref: '#/components/schemas/DeviceCategory'
        bmsDeviceId:
          type: string
          nullable: true
          example: "BMS-0001"
        profileId:
          type: string
          nullable: true
          example: "prf_cm71"
        areaId:
          type: integer
          nullable: true
        buildingId:
          type: integer
          nullable: true
        assetId:
          type: string
          nullable: true
        roomNo:
          type: integer
          nullable: true
        floorNo:
          type: integer
          nullable: true
        sochiotDeviceIds:
          type: array
          items:
            type: integer
          example: [1231]
        templateName:
          type: string
          nullable: true
          example: "EnergyMeter_Template_V1"
        displayOrder:
          type: integer
          example: 0
        isActive:
          type: boolean
          example: true
        installedAt:
          type: string
          format: date-time
          nullable: true
        lastSeenAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        settings:
          type: array
          items:
            $ref: '#/components/schemas/DeviceSetting'
        widgets:
          type: array
          items:
            $ref: '#/components/schemas/DeviceWidget'
        rules:
          type: array
          items:
            $ref: '#/components/schemas/DeviceRule'

    DeviceSummary:
      type: object
      properties:
        totalDevices:
          type: integer
          example: 14
        activeDevices:
          type: integer
          example: 12
        inactiveDevices:
          type: integer
          example: 2
        byCategory:
          type: object
          example:
            ENERGY_METER: 8
            HVAC: 4
            DIESEL_GENERATOR: 2

    DeviceLatestEventField:
      type: object
      properties:
        eventFieldName:
          type: string
          example: "3,184"
        eventFieldDisplayName:
          type: string
          example: "EB KVAH"
        unit:
          type: string
          nullable: true
          example: "KVAH"
        time:
          type: integer
          example: 1787826287264
        fieldCurrentValue:
          nullable: true
          example: 2038.7
        timeFormatted:
          type: string
          example: "27 Aug 2026 03:54 pm"

    DeviceLatestEventModule:
      type: object
      properties:
        buildingName:
          type: string
          example: "Noida- 63"
        deviceId:
          type: string
          example: "171ec6bd-ead4-4920-8884-ac05720a7990"
        eventFields:
          type: array
          items:
            $ref: '#/components/schemas/DeviceLatestEventField'
        locationId:
          type: integer
          example: 63
        moduleId:
          type: integer
          example: 4709
        moduleType:
          type: string
          example: "general_2023-01-11 14:28:03.234"
        lastModuleEvent:
          type: integer
          example: 1787826287264
        lastModuleEventTimeFormatted:
          type: string
          example: "27 Aug 2026 03:54 pm"

    DeviceSetting:
      type: object
      properties:
        id:
          type: integer
          example: 101
        deviceId:
          type: integer
          example: 10
        moduleId:
          type: integer
          nullable: true
          example: 4583
        moduleName:
          type: string
          nullable: true
          example: "Main Incomer"
        sochiotFieldId:
          type: integer
          nullable: true
        sochiotFieldName:
          type: string
          example: "3,100F"
        displayName:
          type: string
          example: "Voltage R-N"
        dataType:
          $ref: '#/components/schemas/FieldDataType'
        unit:
          type: string
          nullable: true
          example: "V"
        enumValues:
          type: array
          items:
            type: string
        warningHigh:
          type: number
          nullable: true
          example: 250.0
        criticalHigh:
          type: number
          nullable: true
          example: 260.0
        warningLow:
          type: number
          nullable: true
          example: 210.0
        criticalLow:
          type: number
          nullable: true
          example: 200.0
        isCommand:
          type: boolean
          example: false
        commandAlias:
          type: string
          nullable: true
        isReadable:
          type: boolean
          example: true
        isDisplayed:
          type: boolean
          example: true
        graphable:
          type: boolean
          example: true
        displayOrder:
          type: integer
          example: 1
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    DeviceWidget:
      type: object
      properties:
        id:
          type: integer
          example: 1
        deviceId:
          type: integer
          example: 10
        widgetId:
          type: string
          example: "E1"
        displayName:
          type: string
          example: "Total Active Power"
        bmsName:
          type: string
          nullable: true
        moduleId:
          type: integer
          nullable: true
          example: 4583
        eventId:
          type: integer
          nullable: true
        eventKey:
          type: string
          nullable: true
          example: "4,0F"
        isActive:
          type: boolean
          example: true
        telemetryType:
          $ref: '#/components/schemas/TelemetryType'
        deviationRequired:
          type: boolean
          example: false
        lowerValue:
          type: number
          nullable: true
        upperValue:
          type: number
          nullable: true
        alarmRequired:
          type: boolean
          example: false
        reportType:
          $ref: '#/components/schemas/ReportWidgetType'
        samplingInterval:
          $ref: '#/components/schemas/SamplingInterval'
        downloadable:
          type: boolean
          example: false
        firmware:
          type: string
          nullable: true
        dashboardView:
          type: string
          nullable: true
        parentWidgetId:
          type: string
          nullable: true
        displayOrder:
          type: integer
          example: 1
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    DeviceRule:
      type: object
      properties:
        id:
          type: integer
          example: 15
        deviceId:
          type: integer
          example: 10
        name:
          type: string
          example: "VOLTAGE_HIGH_RULE"
        description:
          type: string
          nullable: true
        ruleType:
          type: string
          example: "CONDITION"
        sochiotModuleId:
          type: integer
          example: 4583
        isActive:
          type: boolean
          example: true
        priority:
          type: integer
          example: 1
        fields:
          type: array
          items:
            $ref: '#/components/schemas/DeviceRuleField'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    DeviceRuleField:
      type: object
      properties:
        id:
          type: integer
          example: 120
        ruleId:
          type: integer
          example: 15
        fieldName:
          type: string
          example: "condition_type"
        displayName:
          type: string
          example: "Condition Type"
        fieldGroup:
          type: string
          nullable: true
          example: "CONDITION"
        moduleFieldMappingId:
          type: integer
          nullable: true
          example: 28135
        settingFieldId:
          type: integer
          nullable: true
        sochiotFieldName:
          type: string
          nullable: true
        value:
          type: string
          nullable: true
          example: "MODBUS"
        dataType:
          type: string
          example: "TEXT_SHORT"
        supportedValues:
          type: array
          items:
            type: string
        isRequired:
          type: boolean
          example: true
        displayOrder:
          type: integer
          example: 0

    DeviceCommand:
      type: object
      properties:
        id:
          type: integer
          example: 50
        siteId:
          type: integer
          example: 7
        deviceId:
          type: integer
          example: 10
        requestedByUserId:
          type: string
          nullable: true
          example: "usr_123"
        source:
          $ref: '#/components/schemas/ActionSource'
        fieldKey:
          type: string
          example: "SET_RELAY_STATE"
        commandValue:
          type: string
          example: "ON"
        status:
          $ref: '#/components/schemas/CommandStatus'
        sentAt:
          type: string
          format: date-time
        acknowledgedAt:
          type: string
          format: date-time
          nullable: true
        failedAt:
          type: string
          format: date-time
          nullable: true
        errorMessage:
          type: string
          nullable: true

    ModuleActionLog:
      type: object
      properties:
        id:
          type: integer
          example: 200
        siteId:
          type: integer
          example: 7
        deviceId:
          type: integer
          example: 10
        action:
          type: string
          example: "UPDATE_THRESHOLD"
        source:
          $ref: '#/components/schemas/ActionSource'
        performedByUserId:
          type: string
          nullable: true
        previousValue:
          type: string
          nullable: true
        newValue:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time

    # --- REQUEST BODY SCHEMAS ---
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
        rememberMe:
          type: boolean

    AuthUser:
      type: object
      properties:
        name:
          type: string
          example: "Super Admin"
        email:
          type: string
          format: email
          example: "sa@ismartaccess.com"
        role:
          $ref: '#/components/schemas/UserRole'
        permissions:
          type: array
          items:
            type: string
          example: ["*"]
        features:
          type: object
          additionalProperties:
            type: boolean
        zoneLocations:
          type: array
          items:
            type: object
        scopeType:
          type: string
          example: "TENANT"
        scopeId:
          type: string
          example: "cmsfq874j0002bsiaumzb92j7"

    AuthResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: object
          properties:
            user:
              $ref: '#/components/schemas/AuthUser'
            accessToken:
              type: string
              example: "eyJhbGciOiJIUzI1Ni..."
            refreshToken:
              type: string
              example: "7f9a8b1c2d3e4f5a6b7c8d9e0f..."
            expiresIn:
              type: integer
              example: 900

    CreateUserRequest:
      type: object
      required: [name, email, role, roleId]
      properties:
        name:
          type: string
        email:
          type: string
          format: email
        role:
          $ref: '#/components/schemas/UserRole'
        roleId:
          type: integer
        tenantId:
          type: string
        features:
          type: object
          description: Feature permission flags for the user
          properties:
            dashboard:
              type: boolean
              example: true
            reports:
              type: boolean
              example: true
            alarm:
              type: boolean
              example: true
            ackAlarm:
              type: boolean
              example: true
            ticket:
              type: boolean
              example: true
            serviceHistory:
              type: boolean
              example: true
            dpr:
              type: boolean
              example: true
        zoneLocations:
          type: array
          items:
            type: object
            properties:
              zoneNodeType:
                type: string
              zoneNodeId:
                type: string

    UpdateUserRequest:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 6
          description: New password for the user
        phone:
          type: string
        employeeId:
          type: string
        role:
          $ref: '#/components/schemas/UserRole'
        roleId:
          type: integer
        status:
          $ref: '#/components/schemas/UserStatus'
        features:
          type: object
        zoneLocations:
          type: array
          items:
            type: object
            properties:
              zoneNodeType:
                type: string
              zoneNodeId:
                type: string

    CreateInvitationRequest:
      type: object
      required: [email, role]
      properties:
        email:
          type: string
          format: email
        role:
          $ref: '#/components/schemas/UserRole'
        invitedBy:
          type: string

    CreateTenantRequest:
      type: object
      required: [companyId, name, email]
      properties:
        companyId:
          type: string
          description: ID of the parent company
        name:
          type: string
        email:
          type: string
          format: email
        phone:
          type: string
        address:
          type: string
        subscription:
          type: string
          enum: [BASIC, PREMIUM, ENTERPRISE]
          default: BASIC
        subscriptionPeriod:
          type: string
          enum: [MONTHLY, QUARTERLY, ANNUALLY]
          default: MONTHLY
        licenseValidity:
          type: string
          format: date-time
        sochiotOrgId:
          type: integer
        features:
          type: object
        dashboardTemplate:
          type: string
          default: V1
        status:
          type: string
          enum: [ACTIVE, INACTIVE, SUSPENDED]
          default: ACTIVE

    UpdateTenantRequest:
      type: object
      properties:
        companyId:
          type: string
        name:
          type: string
        email:
          type: string
          format: email
        phone:
          type: string
        address:
          type: string
        subscription:
          type: string
          enum: [BASIC, PREMIUM, ENTERPRISE]
        subscriptionPeriod:
          type: string
          enum: [MONTHLY, QUARTERLY, ANNUALLY]
        licenseValidity:
          type: string
          format: date-time
        sochiotOrgId:
          type: integer
        features:
          type: object
        dashboardTemplate:
          type: string
        status:
          type: string
          enum: [ACTIVE, INACTIVE, SUSPENDED]

    CreateZoneRequest:
      type: object
      required: [tenantId, name]
      properties:
        tenantId:
          type: string
        name:
          type: string
        region:
          type: string
        timezone:
          type: string
        country:
          type: string
        description:
          type: string
        features:
          type: object
        dashboardTemplate:
          type: string
          default: V1
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
          default: ACTIVE

    UpdateZoneRequest:
      type: object
      properties:
        name:
          type: string
        region:
          type: string
        timezone:
          type: string
        country:
          type: string
        description:
          type: string
        features:
          type: object
        dashboardTemplate:
          type: string
        status:
          type: string
          enum: [ACTIVE, INACTIVE]

    CreateTenantAreaRequest:
      type: object
      required: [name]
      properties:
        tenantId:
          type: string
        zoneId:
          type: string
        name:
          type: string
        description:
          type: string
        features:
          type: object
        dashboardTemplate:
          type: string
          default: V1
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
          default: ACTIVE

    UpdateTenantAreaRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        features:
          type: object
        dashboardTemplate:
          type: string
        status:
          type: string
          enum: [ACTIVE, INACTIVE]

    CreateSiteRequest:
      type: object
      required: [name, sochiotLocationId, organizationId]
      properties:
        name:
          type: string
        sochiotLocationId:
          type: integer
        organizationId:
          type: integer
        tenantId:
          type: string
        zoneId:
          type: string
        areaId:
          type: string

    UpdateSiteRequest:
      type: object
      properties:
        name:
          type: string
        sochiotLocationId:
          type: integer
        organizationId:
          type: integer
        organizationType:
          type: string
        address:
          type: string
        city:
          type: string
        state:
          type: string
        pincode:
          type: string
        latitude:
          type: number
        longitude:
          type: number
        timezone:
          type: string
        isActive:
          type: boolean
          description: Active status of the site. Setting to false soft-deletes/disables the site.
        tenantId:
          type: string
        zoneId:
          type: string
        areaId:
          type: string

    CreateBuildingRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        code:
          type: string
        totalFloors:
          type: integer
        description:
          type: string
        isActive:
          type: boolean
          default: true
        displayOrder:
          type: integer
          default: 0

    UpdateBuildingRequest:
      type: object
      properties:
        name:
          type: string
        code:
          type: string
        totalFloors:
          type: integer
        description:
          type: string
        isActive:
          type: boolean
        displayOrder:
          type: integer

    CreateAssetRequest:
      type: object
      required: [name, assetType]
      properties:
        name:
          type: string
          example: "Area 1 - Floor 1"
        assetType:
          $ref: '#/components/schemas/AssetType'
        siteId:
          type: integer
          description: Optional if provided in path URL
          example: 7
        description:
          type: string
          example: "Primary workspace area on Floor 1"
        parentId:
          type: string
          nullable: true
          example: "floor_1"
        order:
          type: integer
          default: 0
          example: 1
        metadata:
          type: object
          example:
            capacity: 50
            sqft: 2400
        status:
          $ref: '#/components/schemas/AssetStatus'
          default: ACTIVE
        installDate:
          type: string
          format: date-time
        installBy:
          type: string
        lastVisitDate:
          type: string
          format: date-time
        firmware:
          type: string
        serialNumber:
          type: string
        sochiotDeviceIds:
          type: array
          items:
            type: integer
          example: [1231]
        widgetConfig:
          type: object

    UpdateAssetRequest:
      type: object
      properties:
        name:
          type: string
          example: "Area 1 - Floor 1 Updated"
        assetType:
          $ref: '#/components/schemas/AssetType'
        description:
          type: string
          example: "Updated workspace description"
        parentId:
          type: string
          nullable: true
          example: "floor_1"
        order:
          type: integer
          example: 2
        metadata:
          type: object
          example:
            capacity: 60
            sqft: 2400
        status:
          $ref: '#/components/schemas/AssetStatus'
        installDate:
          type: string
          format: date-time
        installBy:
          type: string
        lastVisitDate:
          type: string
          format: date-time
        firmware:
          type: string
        serialNumber:
          type: string
        sochiotDeviceIds:
          type: array
          items:
            type: integer
        widgetConfig:
          type: object

    CreateDeviceRequest:
      type: object
      required: [name, category, profileId, sochiotDeviceIds]
      properties:
        name:
          type: string
          example: "EM_LIVEWIZE_178"
        description:
          type: string
        serialNumber:
          type: string
          example: "9454c5f385e821"
        category:
          $ref: '#/components/schemas/DeviceCategory'
        profileId:
          type: string
          example: "prf_cm71"
        sochiotDeviceIds:
          type: array
          items:
            type: integer
          example: [1231]
        areaId:
          type: integer
        buildingId:
          type: integer
        assetId:
          type: string
          example: "ast_cm71"
        roomNo:
          type: integer
        floorNo:
          type: integer
        templateName:
          type: string
        displayOrder:
          type: integer
        isActive:
          type: boolean
          default: true
        installedAt:
          type: string
          format: date-time

    UpdateDeviceRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        serialNumber:
          type: string
        category:
          $ref: '#/components/schemas/DeviceCategory'
        profileId:
          type: string
        sochiotDeviceIds:
          type: array
          items:
            type: integer
        areaId:
          type: integer
        buildingId:
          type: integer
        assetId:
          type: string
          example: "ast_cm71"
        roomNo:
          type: integer
        floorNo:
          type: integer
        templateName:
          type: string
        displayOrder:
          type: integer
        isActive:
          type: boolean
        installedAt:
          type: string
          format: date-time

    DeviceTemplateRequest:
      type: object
      required: [name, category, sochiotDeviceIds, template_settings]
      properties:
        name:
          type: string
          example: "EM_LIVEWIZE_178"
        category:
          $ref: '#/components/schemas/DeviceCategory'
        sochiotDeviceIds:
          type: array
          items:
            type: integer
          example: [1231]
        serialNumber:
          type: string
        profileId:
          type: string
        areaId:
          type: integer
        buildingId:
          type: integer
        assetId:
          type: string
          example: "ast_cm71"
        templateName:
          type: string
          example: "EnergyMeter_Template_V1"
        template_settings:
          type: array
          items:
            type: object
            required: [moduleId, sochiotFieldName, displayName, dataType]
            properties:
              moduleId:
                type: integer
              sochiotFieldName:
                type: string
              displayName:
                type: string
              dataType:
                $ref: '#/components/schemas/FieldDataType'
              unit:
                type: string
              warningHigh:
                type: number
              criticalHigh:
                type: number
              warningLow:
                type: number
              criticalLow:
                type: number
              isCommand:
                type: boolean
              graphable:
                type: boolean
        rules:
          type: array
          items:
            type: object
            required: [name, fields]
            properties:
              name:
                type: string
              ruleType:
                type: string
              fields:
                type: array
                items:
                  type: object

    UpdateThresholdsRequest:
      type: object
      required: [thresholds]
      properties:
        thresholds:
          type: object
          additionalProperties:
            type: object
            properties:
              warningHigh:
                type: number
              criticalHigh:
                type: number
              warningLow:
                type: number
              criticalLow:
                type: number
          example:
            "3,100F":
              warningHigh: 245
              criticalHigh: 255
              warningLow: 215
              criticalLow: 205

    UpdateDeviceSettingsRequest:
      type: object
      required: [settings]
      properties:
        settings:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              moduleId:
                type: integer
              sochiotFieldName:
                type: string
              displayName:
                type: string
              dataType:
                $ref: '#/components/schemas/FieldDataType'
              unit:
                type: string
              warningHigh:
                type: number
              criticalHigh:
                type: number
              warningLow:
                type: number
              criticalLow:
                type: number
              graphable:
                type: boolean
              isCommand:
                type: boolean

    UpdateRuleRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        ruleType:
          type: string
        priority:
          type: integer
        isActive:
          type: boolean
        fields:
          type: array
          items:
            type: object
            required: [fieldName]
            properties:
              id:
                type: integer
              fieldName:
                type: string
              value:
                type: string
              displayName:
                type: string

    UpdateRuleFieldRequest:
      type: object
      required: [value]
      properties:
        value:
          type: string
          nullable: true
          example: "LESS_THAN"

    SyncRulePayload:
      type: object
      required: [name, id, fieldCOS]
      properties:
        name:
          type: string
          example: "RULE_10"
        id:
          type: integer
          description: Sochiot Module ID
          example: 4583
        fieldCOS:
          type: array
          items:
            type: object
            required: [id, currentValue]
            properties:
              id:
                type: integer
                description: moduleFieldMappingId
                example: 28135
              currentValue:
                type: string
                nullable: true
                example: "2026-06-30T18:30:00.219Z"

    SyncRuleWithFieldsRequest:
      type: object
      required: [fields]
      properties:
        fields:
          type: array
          items:
            type: object
            required: [fieldName, value]
            properties:
              fieldName:
                type: string
                example: "condition_type"
              value:
                type: string
                nullable: true
                example: "MODBUS"

    CreateWidgetRequest:
      type: object
      required: [widgetId, displayName]
      properties:
        widgetId:
          type: string
          example: "E1"
        displayName:
          type: string
          example: "Active Power"
        moduleId:
          type: integer
          example: 4583
        eventId:
          type: integer
        eventKey:
          type: string
          example: "4,0F"
        isActive:
          type: boolean
          default: true
        telemetryType:
          $ref: '#/components/schemas/TelemetryType'
          default: AVERAGE
        reportType:
          $ref: '#/components/schemas/ReportWidgetType'
          default: OVERALL
        samplingInterval:
          $ref: '#/components/schemas/SamplingInterval'
          default: MIN_15
        downloadable:
          type: boolean
          default: false
        displayOrder:
          type: integer
          default: 0

    UpdateWidgetRequest:
      type: object
      properties:
        displayName:
          type: string
        moduleId:
          type: integer
        eventId:
          type: integer
        eventKey:
          type: string
        isActive:
          type: boolean
        telemetryType:
          $ref: '#/components/schemas/TelemetryType'
        reportType:
          $ref: '#/components/schemas/ReportWidgetType'
        samplingInterval:
          $ref: '#/components/schemas/SamplingInterval'
        downloadable:
          type: boolean
        displayOrder:
          type: integer

    BulkCreateWidgetsRequest:
      type: object
      required: [widgets]
      properties:
        widgets:
          type: array
          items:
            $ref: '#/components/schemas/CreateWidgetRequest'

    SyncWidgetsRequest:
      type: object
      required: [widgets]
      properties:
        widgets:
          type: array
          items:
            type: object
            required: [displayName, moduleId, eventId, eventKey]
            properties:
              widgetId:
                type: string
              displayName:
                type: string
              moduleId:
                type: integer
              eventId:
                type: integer
              eventKey:
                type: string

    ReorderWidgetsRequest:
      type: object
      required: [widgetOrders]
      properties:
        widgetOrders:
          type: array
          items:
            type: object
            required: [widgetId, displayOrder]
            properties:
              widgetId:
                type: string
              displayOrder:
                type: integer

    ResyncEventStatsRequest:
      type: object
      required: [id, dateTime]
      properties:
        id:
          type: integer
          example: 10
        dateTime:
          type: string
          format: date-time
          example: "2026-07-24T00:00:00.000Z"

    SendCommandRequest:
      type: object
      required: [commandKey, value]
      properties:
        commandKey:
          type: string
          example: "SET_RELAY_STATE"
        value:
          type: string
          example: "ON"

    SendNotificationRequest:
      type: object
      required: [type, channel, subject, message]
      properties:
        email:
          type: string
        type:
          type: string
        channel:
          type: string
        subject:
          type: string
        message:
          type: string

    GenerateReportRequest:
      type: object
      required: [reportType, startDate, endDate]
      properties:
        reportType:
          type: string
        siteId:
          type: integer
        startDate:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time

    TriggerAlarmRequest:
      type: object
      required: [deviceId, fieldKey, value, severity]
      properties:
        deviceId:
          type: integer
        fieldKey:
          type: string
        value:
          type: number
        severity:
          $ref: '#/components/schemas/AlarmSeverity'
